[project]
name = "matrix-loading"
version = "0.1.0"
description = "Matrix data analysis project"
requires-python = ">=3.10,<4.0"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
]

[tool.poetry]
package-mode = false